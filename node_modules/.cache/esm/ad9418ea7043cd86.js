let mongoose,dotenv,bold;_704‍.x([["default",()=>_704‍.o]]);_704‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_704‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_704‍.w("chalk",[["default",["bold"],function(v){bold=v}]]);



dotenv.config();

const { cyan, yellow, red } = bold;

const connected = cyan;
const error = yellow;
const disconnected = red;

// export this function and imported by server.js

const Connect = () => {
  mongoose.connect(process.env.DB_URI, { useNewUrlParser: true });
  mongoose.set('useFindAndModify', false);
  mongoose.set('useCreateIndex', true);

  mongoose.connection.on('connected', () => {
    _704‍.g.console.log(
      connected(`Mongoose default connection is open to ${process.env.DB_URI}`)
    );
  });

  mongoose.connection.on('error', err => {
    _704‍.g.console.log(error(`Mongoose default connection has occured ${err} error`));
  });

  mongoose.connection.on('disconnected', () => {
    _704‍.g.console.log(disconnected('Mongoose default connection is disconnected'));
  });
};

_704‍.d(Connect);
